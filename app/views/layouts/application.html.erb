<!DOCTYPE html>
<html>
<head>
  <title>Rails3PracticeTdd</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<%= link_to("Home", "/") %>
<% if user_signed_in? and current_user.admin? %>
  <%= link_to("Admin", admin_forums_path) %>
<% end %>

<%= auth_menu %>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>
